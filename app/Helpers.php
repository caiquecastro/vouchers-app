<?php

namespace App;

class Helpers
{
    public static function generateRandomCode()
    {
        // $str = '';

        // $max = mb_strlen($keyspace, '8bit') - 1;

        // for ($i = 0; $i < $length; ++$i) {
        //     $str .= $keyspace[random_int(0, $max)];
        // }
        // return $str;
        return '';
    }
}